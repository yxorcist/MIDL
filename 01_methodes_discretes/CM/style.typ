#let definition(body) = block(
  width: 100%,
  inset: 10pt,
  stroke: 0.6pt,
  radius: 4pt,
  body,
)

#let theorem(body) = block(
  width: 100%,
  inset: 10pt,
  stroke: 0.6pt,
  radius: 4pt,
  body,
)
