#set page(paper: "a4", margin: 2cm)
#set text(lang: "fr", size: 11pt)
#set par(justify: true, leading: 0.65em)

= Exercice 5

#block(stroke: 0.6pt + gray, inset: 8pt)[
  *Notes source incomplètes.*

  Les notes signalent une erreur dans un raisonnement par recouvrement de deux groupes $G_1$ et $G_2$.

  L'idée visible est que les deux groupes doivent avoir une intersection non vide,
  afin de transmettre une même propriété d'un groupe à l'autre.

  La note manuscrite indique notamment qu'il fallait traiter explicitement le cas $n=2$.

  L'énoncé complet de l'exercice et la propriété commune aux éléments des groupes ne sont pas visibles sur la photo ; ils ne sont donc pas reconstruits ici.
]
