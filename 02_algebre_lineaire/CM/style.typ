// Macros réutilisables du cours.

#let definition(body) = block[
  *Définition.* #body
]

#let theorem(body) = block[
  *Théorème.* #body
]

#let proposition(body) = block[
  *Proposition.* #body
]

#let example(body) = block[
  *Exemple.* #body
]

#let remark(body) = block[
  *Remarque.* #body
]
